import { ConsultaStatusPipe } from './consulta-status.pipe';

describe('ConsultaStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultaStatusPipe();
    expect(pipe).toBeTruthy();
  });
});

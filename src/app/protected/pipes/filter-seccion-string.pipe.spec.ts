import { FilterSeccionStringPipe } from './filter-seccion-string.pipe';

describe('FilterSeccionStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeccionStringPipe();
    expect(pipe).toBeTruthy();
  });
});

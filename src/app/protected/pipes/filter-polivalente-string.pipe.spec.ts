import { FilterPolivalenteStringPipe } from './filter-polivalente-string.pipe';

describe('FilterPolivalenteStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPolivalenteStringPipe();
    expect(pipe).toBeTruthy();
  });
});

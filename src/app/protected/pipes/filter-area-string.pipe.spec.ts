import { FilterAreaStringPipe } from './filter-area-string.pipe';

describe('FilterAreaStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAreaStringPipe();
    expect(pipe).toBeTruthy();
  });
});

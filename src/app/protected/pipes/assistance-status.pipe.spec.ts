import { AssistanceStatusPipe } from './assistance-status.pipe';

describe('AssistanceStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AssistanceStatusPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterUserStringPipe } from './filter-user-string.pipe';

describe('FilterUserStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserStringPipe();
    expect(pipe).toBeTruthy();
  });
});

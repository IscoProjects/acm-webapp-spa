import { StatusManagementPipe } from './status-management.pipe';

describe('StatusManagementPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusManagementPipe();
    expect(pipe).toBeTruthy();
  });
});
